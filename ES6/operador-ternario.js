var n = Math.round(20*Math.random()-10);

if(n<0) {
  console.log("El numero es negativo" + n);
} else {

(n % 2) == 0 ? console.log("numero par") : console.log("numero impar");

  /*if(n % 2 == 0)
    console.log("El numero es par" + n);
  else
    console.log("El numero es impar" + n); */
}
